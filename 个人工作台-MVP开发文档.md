# 个人工作台 MVP 开发文档

## 1. 项目概述

这是一个面向个人使用的云端工作台，主要运行在 Mac 和 Windows 的桌面浏览器，同时兼容 iPhone Safari。

核心目标是帮助用户持续记录：

- 今天计划做什么；
- 实际完成了什么；
- 学到了什么；
- 遇到了什么问题；
- 当天状态和情绪如何；
- 明天应该如何调整。

电脑和手机登录同一账号后，数据自动同步。

## 2. 最简部署方案

第一版不购买、不架设个人服务器，采用托管服务：

| 服务 | 用途 | 第一版建议 |
|---|---|---|
| Vercel | 部署网页前端和服务端接口 | 免费层起步 |
| Supabase | 登录、PostgreSQL 数据库、权限控制 | 免费层起步 |
| GitHub | 保存代码和自动部署 | 免费 |
| 自定义域名 | 可选，不影响使用 | 初期不购买 |
| AI 模型 API | 生成日报和复盘建议 | 第二阶段接入，按调用量计费 |

访问地址先使用 Vercel 提供的域名即可。以后需要更专业的地址时，再购买域名并绑定，不需要迁移服务器。

## 3. 推荐技术架构

- 前端：Next.js + TypeScript
- 页面样式：Tailwind CSS
- 数据库：Supabase PostgreSQL
- 登录：Supabase Auth，优先支持邮箱验证码或魔法链接
- 数据同步：所有记录保存到 Supabase，页面加载时读取云端数据
- 权限：每个用户只能读取和修改自己的数据，使用 Supabase RLS
- 部署：GitHub 推送后由 Vercel 自动部署
- AI 接口：通过服务端 API Route 或 Supabase Edge Function 调用，绝不把模型密钥写入浏览器代码

## 4. MVP 功能范围

### 4.1 登录与账号

- 邮箱登录；
- 登录状态保持；
- 退出登录；
- 未登录用户不能访问个人记录；
- 同一账号在电脑和手机上看到相同数据。

### 4.2 今日工作台

今日页是首页，重点是快速记录和查看，不做营销式首页。

包含：

- 日期和今日状态；
- 今日最重要的 1～3 件事；
- 任务列表；
- 快速添加工作记录；
- 快速添加学习记录；
- 情绪/精力状态；
- 今日完成度；
- “开始复盘”入口。

桌面端采用左右分栏：左侧为导航和日期，右侧为当天内容。手机端改为单列纵向布局，底部提供主要导航。

### 4.3 任务

任务字段：

- 标题；
- 描述，可选；
- 日期；
- 优先级：高、中、低；
- 状态：未开始、进行中、已完成、已取消；
- 来源：手动创建或从工作记录转换；
- 创建时间和更新时间。

操作：

- 新建、编辑、完成、取消、删除；
- 按日期查看；
- 将未完成任务顺延到下一天；
- 首页只显示当天任务。

### 4.4 工作记录

用于回答“今天做了什么”。

字段：

- 标题；
- 内容；
- 所属日期；
- 关联任务，可选；
- 产出或结果，可选；
- 标签，可选；
- 创建时间和更新时间。

### 4.5 学习记录

用于回答“今天学到了什么”。

字段：

- 标题；
- 学习内容；
- 来源链接，可选；
- 关键要点；
- 下一步行动；
- 所属日期；
- 创建时间和更新时间。

### 4.6 每日复盘

复盘表单保持短小，避免用户因为填写复杂而放弃。

字段：

- 今天完成了什么；
- 今天最重要的收获；
- 今天遇到的阻碍；
- 哪件事可以做得更好；
- 明天最重要的一件事；
- 情绪：低落、一般、平稳、不错、很好；
- 精力：1～5；
- 自由备注。

保存后显示一张简洁的复盘摘要。

### 4.7 AI 总结（第二阶段）

AI 不参与第一版核心数据保存，先保证手动记录和同步可靠。

第二阶段增加：

- 根据当天任务、工作记录、学习记录生成日报；
- 提取完成事项和未完成事项；
- 归纳重复出现的问题；
- 给出明日行动建议；
- 用户确认后才能写入正式记录；
- 支持重新生成，但不覆盖原始记录。

模型调用通过服务端完成，API 密钥只保存于 Vercel/Supabase 的环境变量中。

### 4.8 历史回顾

- 按日期查看每日工作台；
- 按关键词搜索记录；
- 按标签筛选；
- 查看连续复盘；
- 显示近 7 天和近 30 天的完成情况；
- 先做简单统计，不做复杂数据大屏。

## 5. 数据模型

建议使用以下表：

### profiles

- `id`：关联 Supabase Auth 用户 ID；
- `display_name`；
- `timezone`，默认 `Asia/Shanghai`；
- `created_at`；
- `updated_at`。

### tasks

- `id`；
- `user_id`；
- `title`；
- `description`；
- `task_date`；
- `priority`；
- `status`；
- `created_at`；
- `updated_at`。

### work_entries

- `id`；
- `user_id`；
- `entry_date`；
- `title`；
- `content`；
- `result`；
- `task_id`，可为空；
- `tags`；
- `created_at`；
- `updated_at`。

### learning_entries

- `id`；
- `user_id`；
- `entry_date`；
- `title`；
- `content`；
- `source_url`；
- `key_points`；
- `next_action`；
- `created_at`；
- `updated_at`。

### daily_reviews

- `id`；
- `user_id`；
- `review_date`，每个用户每天最多一条；
- `completed_summary`；
- `main_gain`；
- `blockers`；
- `improvement`；
- `tomorrow_focus`；
- `mood`；
- `energy`；
- `notes`；
- `created_at`；
- `updated_at`。

所有业务表都必须启用 RLS，并添加“用户只能访问自己 `user_id` 数据”的策略。

## 6. 页面结构

### 桌面端

- `/login`：登录页；
- `/today`：今日工作台；
- `/tasks`：任务管理；
- `/records`：工作和学习记录；
- `/reviews`：复盘历史；
- `/settings`：账号、时区和数据设置。

### 手机端

- 采用单列布局；
- 底部导航：今日、任务、记录、复盘；
- 新建记录使用底部抽屉或全屏表单；
- 主要按钮固定在易于拇指操作的位置；
- 表格改为卡片和列表，不使用需要横向滚动的大表格。

## 7. 同步与离线策略

第一版采用“云端优先”：

- 每次保存后写入 Supabase；
- 页面重新打开时从云端读取；
- 保存按钮显示保存中、已保存、失败三种状态；
- 网络失败时保留表单内容，不清空用户输入；
- 第一版不做复杂的离线冲突合并；
- 可增加浏览器本地草稿，网络恢复后由用户手动重试保存。

## 8. 安全与隐私

- 不在前端暴露 Supabase service role key；
- 模型 API 密钥只能放在服务端环境变量；
- 所有业务表启用 RLS；
- 登录链接不写入日志；
- 删除记录前二次确认；
- 提供导出个人数据功能，格式为 JSON 或 Markdown；
- 提供删除账号及其数据的入口，正式上线前需完成测试。

## 9. 开发顺序

### 阶段 1：基础工程

- 创建 Next.js 项目；
- 配置 TypeScript、Tailwind CSS 和基础布局；
- 创建 Supabase 项目；
- 配置环境变量；
- 完成邮箱登录和退出登录；
- 配置数据库表和 RLS。

### 阶段 2：核心记录

- 完成今日工作台；
- 完成任务增删改查；
- 完成工作记录和学习记录；
- 完成每日复盘；
- 完成桌面端和手机端响应式布局。

### 阶段 3：同步和历史

- 验证 Mac、Windows 和 iPhone Safari 登录同一账号；
- 完成日期切换；
- 完成历史回顾和搜索；
- 增加保存状态和网络错误提示；
- 增加 JSON/Markdown 导出。

### 阶段 4：AI 功能

- 增加服务端 AI 接口；
- 生成日报和复盘摘要；
- 增加用户确认后写入机制；
- 记录模型调用错误，但不记录敏感原文到服务端日志。

## 10. MVP 验收标准

- 用户可以注册或登录；
- 用户可以在今日页创建任务、工作记录和学习记录；
- 用户可以完成每日复盘并按日期查看；
- Mac 和 Windows 浏览器上的数据保持一致；
- iPhone Safari 可以完成查看、新建、编辑和复盘；
- 一个用户无法看到另一个用户的数据；
- 网络失败时不会丢失当前输入；
- 页面在桌面和手机宽度下均无横向溢出；
- 项目可以通过 Vercel 部署并使用 HTTPS 访问；
- AI 功能未接入时，核心记录和同步功能仍然完整可用。

## 11. 成本和运维说明

第一版可以不购买个人服务器，也不需要维护操作系统、数据库备份或 HTTPS 证书。

可能产生费用的项目：

- 自定义域名：可选；
- AI 模型调用：按使用量计费；
- 超出 Vercel 或 Supabase 免费额度后的托管费用；
- 备份和更高存储容量：数据量增大后再考虑。

上线前应重新查看 Vercel、Supabase 和模型服务商当前的价格与免费额度，不能把免费额度视为永久不变。

## 12. 暂不做的功能

- 团队协作；
- 多角色权限；
- 复杂项目管理；
- 邮件、日历和第三方连接器；
- 原生 iOS/Windows 客户端；
- 复杂的 AI 自动执行；
- 复杂图表和绩效评分；
- 多人实时编辑。

## 13. 与视频内容的对应关系

视频明确表达的核心需求是：记录一天做过什么、学到了什么，晚上复盘，长期观察自己的进步，并在情绪出现时暂停和自我觉察。

本开发文档将这些内容转化为：

- 工作记录；
- 学习记录；
- 每日复盘；
- 情绪和精力字段；
- 历史回顾；
- 后续 AI 日报和建议。

视频没有明确说明的账号、数据库、同步、权限和备份部分，属于为了实现跨设备个人工作台而补充的工程需求。
